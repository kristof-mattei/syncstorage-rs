name: rust-clippy-spanner
runs:
  using: composite
  steps:
  - name: Rust Clippy Spanner
    run: cargo clippy --workspace --all-targets --no-default-features --features=syncstorage-db/spanner --features=py_verifier -- -D warnings
    shell: bash